<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="8dp">

    <!-- Main Collapsed Circular Button -->
    <FrameLayout
        android:id="@+id/btnMainCircle"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:background="@drawable/bg_floating_circle"
        android:elevation="8dp"
        android:clickable="true"
        android:focusable="true">

        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="center"
            android:src="@android:drawable/ic_menu_camera"
            android:contentDescription="Recorder Hub" />
    </FrameLayout>

    <!-- Expanded Control Container -->
    <LinearLayout
        android:id="@+id/layoutExpandedControls"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:visibility="gone"
        android:background="@drawable/bg_floating_container"
        android:elevation="8dp"
        android:padding="6dp"
        android:gravity="center">

        <!-- Start / Record Button (Play ▶) -->
        <ImageButton
            android:id="@+id/btnStart"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_play"
            android:contentDescription="Start Recording" />

        <!-- Pause / Resume Button (Pause ❚❚ / Play ▶) -->
        <ImageButton
            android:id="@+id/btnPauseResume"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="4dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_pause"
            android:contentDescription="Pause or Resume Recording" />

        <!-- Stop Button (Stop ■) -->
        <ImageButton
            android:id="@+id/btnStop"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_stop"
            android:contentDescription="Stop and Save Recording" />

    </LinearLayout>

</FrameLayout>
